function x = vec(x)
% VEC Reshapes the input into a column vector
%
%   Inputs:
%       x - A matrix or an array
%
%   Outputs:
%       x - A column vector obtained by reshaping the input

x = x(:);

end
